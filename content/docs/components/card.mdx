---
title: Card
description: Displays a card with header, content, and footer.
---

## Import

```tsx
import {
    Card,
    CardAction,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card"
```

## Usage

```tsx
<Card>
    <CardHeader>
        <CardTitle>Card Title</CardTitle>
        <CardDescription>Card description text.</CardDescription>
    </CardHeader>
    <CardContent>
        <p>Card content goes here.</p>
    </CardContent>
    <CardFooter>
        <Button>Action</Button>
    </CardFooter>
</Card>
```

## Components

| Component         | Description                                  |
| ----------------- | -------------------------------------------- |
| `Card`            | Root container with shadow and border radius |
| `CardHeader`      | Container for title, description, and action |
| `CardTitle`       | Card heading                                 |
| `CardDescription` | Muted description text                       |
| `CardAction`      | Action element positioned in the header      |
| `CardContent`     | Main content area                            |
| `CardFooter`      | Footer with flex layout for actions          |

## Sizes

Cards support `default` and `sm` sizes:

```tsx
<Card>Default card</Card>
<Card size="sm">Small card</Card>
```

Small cards reduce padding and gap spacing.

## With Image

When an `<img>` is the first child, the card automatically removes top padding and applies rounded corners to the image:

```tsx
<Card>
    <img src="/image.jpg" alt="Cover" />
    <CardHeader>
        <CardTitle>Title</CardTitle>
    </CardHeader>
    <CardContent>
        <p>Content with cover image.</p>
    </CardContent>
</Card>
```

## With Action

```tsx
<Card>
    <CardHeader>
        <CardTitle>Settings</CardTitle>
        <CardAction>
            <Button variant="outline" size="icon-sm">
                <HugeiconsIcon icon={SettingsIcon} />
            </Button>
        </CardAction>
    </CardHeader>
    <CardContent>...</CardContent>
</Card>
```
